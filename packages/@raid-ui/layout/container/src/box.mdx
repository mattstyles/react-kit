---
title: Layout/Box
section: Components
---

import { Live, Repos } from '@internal/docs'
import { Badge, Stack } from '@raid/kit'

# Box

Responsive box layout component.

The box layout component often forms the base layer from which other components extend. It defaults to rendering a `div` element.

## Example

<Live code={`
<Box p={6} bg='primary' />
`} />

## Install

```sh
npm install @raid/kit
```

```js
import { Box } from '@raid/kit'
```

<Repos repos={['@raid/kit', '@raid-ui/layout', '@raid-ui/container']} />
